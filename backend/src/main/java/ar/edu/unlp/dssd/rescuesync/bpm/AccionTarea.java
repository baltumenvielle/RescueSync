package ar.edu.unlp.dssd.rescuesync.bpm;

/** Pantalla de la SPA que resuelve cada tarea humana conocida del proceso. */
public enum AccionTarea {
    REGISTRO, REVISION, DESGLOSE, EVALUAR_COBERTURA, OTRA
}
